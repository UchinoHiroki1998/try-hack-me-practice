version: "3.8"

services:
  kali:
    image: kalilinux/kali-rolling
    container_name: kali
    stdin_open: true        # コンテナにアタッチするため
    tty: true               # ターミナルを有効にする
    volumes:
      - ./data:/root/workspace  # ホストと共有するディレクトリ
    command: /bin/bash     # 起動時にbashを使う
